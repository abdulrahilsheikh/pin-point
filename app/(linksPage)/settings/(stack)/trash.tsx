import { View, Text, SafeAreaView } from "react-native";
import React from "react";
import { ThemedText } from "@/components/ThemedText";

const index = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <ThemedText>ASeew Link</ThemedText>
    </SafeAreaView>
  );
};

export default index;
